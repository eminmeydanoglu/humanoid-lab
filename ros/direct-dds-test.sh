#!/usr/bin/env bash
# ROS Stage A direct-DDS test (TEMPLATE).
# Intentionally empty until robot access parameters exist; a "fail loudly" placeholder.
set -euo pipefail

echo "error: direct-dds-test.sh not implemented — requires robot access parameters" >&2
echo "       (NIC/IP, ROS_DOMAIN_ID, RMW, interface repo). See ros/README.md (Stage A)." >&2
exit 2